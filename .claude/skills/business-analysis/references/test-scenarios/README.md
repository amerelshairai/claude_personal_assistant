# Test scenarios — business-analysis

Fully synthetic, same pattern as `client-intelligence` and the time-orchestration
trio. Each folder has `input.md` (synthetic client/project state) and `output.md`
(the full business-analysis walkthrough). Scenarios are deliberately varied by
project type (greenfield / feature-addition / bug-fix) and each is built to actually
stress a viability dimension, not just produce a clean pass.

Not real data. Safe to delete once validated (ask Amer first, per the no-delete
rule).
